

export function PaymentModal({open ,onClose, order}){
    return(
        <div className={`absolute top-0 w-full h-full bg-white ${ !open ? "none": "" } z-999`}>
           sdasda
        </div>
    )
}